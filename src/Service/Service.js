let LoginDetails =[];
let patient_id = null
let selectedDate = null;
let selectedTime = null;
let patientList =[];

export default {LoginDetails, patient_id, selectedDate, selectedTime, patientList}